<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.streuobstpfad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FlussActivity"></activity>
        <activity android:name=".BilderPuzzleActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".PuzzleActivity" />
        <activity android:name=".MemoryActivity" />
        <activity android:name=".BilderActivity" />
        <activity android:name=".HochStammActivity" />
        <activity android:name=".SchleiereuleActivity" />
        <activity android:name=".FrBrummerActivity" />
        <activity android:name=".settingActivity" />
        <activity android:name=".searchActivity" />
        <activity android:name=".SteinhaufenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>